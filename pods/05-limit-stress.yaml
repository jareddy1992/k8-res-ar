apiVersion: v1
kind: Pod
metadata:
  name: stress
  namespace: roboshop
spec:
  containers:
  - name: stress
    image: jareddy1992/stress:v2
    command: ["sleep","200"]
    resources: 
     requests:
      cpu: "150m"
      memory: "256Mi"
    limits:
      cpu: "200m"
      memory: "300Mi"
